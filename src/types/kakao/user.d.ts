interface UserResponseDto {
  userId: string;
  name: string;
  message: string;
  baseUrl: string;
  backgroundUrl: string;
}

interface UserInfo {
  userId: string;
  name: string;
  message: string;
  baseUrl: string;
  backgroundUrl: string;
}
